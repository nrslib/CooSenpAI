{
  "name": "@coosenpai/provider-bridge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build": "esbuild src/bridge.ts --bundle --platform=node --format=esm --target=node18 --outfile=dist/bridge.js --banner:js=\"import { createRequire as __coosenpaiCreateRequire } from 'node:module'; const require = __coosenpaiCreateRequire(import.meta.url);\"",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "0.3.206",
    "@openai/codex-sdk": "0.147.0",
    "@opencode-ai/sdk": "1.18.2"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
