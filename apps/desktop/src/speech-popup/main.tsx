import React from "react";
import ReactDOM from "react-dom/client";
import { SpeechPopup } from "./SpeechPopup.js";
import "./styles.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode><SpeechPopup /></React.StrictMode>,
);
